type: pages
headline: Projects
parent: site.find("projects")
size: tiny
info: "{{ page.categories }}"
layout: cards
template: project
empty: No projects yet
image:
  query: page.cover.toFile # todo: fix this (I think it's fixed actually)
  cover: true
  ratio: 5/4
